//#10250
#include <iostream>
using namespace std;

int main(void)
{
    int n = 0;
    cin >> n;
    int *H = new int[n];
    int *W = new int[n];
    int *N = new int[n];
    for (int i = 0; i < n; i++)
    {
        cin >> H[i] >> W[i] >> N[n];
    }

    for (int i = 0; i < n; i++)
    {
        int index1 = H[i];
        int index2 = W[i];
    }
    return 0;
}