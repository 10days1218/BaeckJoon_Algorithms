//#2750
#include <iostream>
#include <vector>
using namespace std;
int main(void)
{
    int num = 0;
    vector<int> v;
    int i_min = 0;
    cin >> num;
    v.resize(num);

    for (int i = 0; i < num; i++)
    {
        cin >> v[i];
    }

    for (int i = 0; i < num - 1; i++)
    {
        i_min = 0;
        for (int)
    }
}