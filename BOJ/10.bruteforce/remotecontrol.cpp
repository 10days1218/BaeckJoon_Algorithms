//#1107
#include <bits/stdc++.h>
using namespace std;

int remote[10];
int main(void)
{
    int n, m;
    cin >> n;
    cin >> m;

    for (int i = 0; i < m; i++)
    {
        int number;
        cin >> number;
        remote[number] = 1;
    }

    for (int i = 0; i <= 500000; i++)
    {
        ;
    }

    return 0;
}