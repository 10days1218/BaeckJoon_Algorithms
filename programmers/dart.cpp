#include <string>

using namespace std;

int solution(string dartResult)
{
    int answer = 0;
    int arr[3] = {
        0,
    };

    for (int i = 0; i < dartResult.length(); i++)
    {
        if (dartResult[i] >= '0' && dartResult[i] <= '9')
        {
            ;
        }
    }
    return answer;
}