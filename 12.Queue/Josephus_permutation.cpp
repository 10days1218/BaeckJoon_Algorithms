//#11866
#include <iostream>
#include <vector>
using namespace std;

int main(void)
{
    int N, M;
    vector<int> v;
    cin >> N >> M;
    int index = 0;
    v.resize(N);

    for (int i = 0; i < N; i++)
        v[i] = i + 1;

    while (1)
    {
        ;
    }
    return 0;
}