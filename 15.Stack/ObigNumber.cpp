//#17298
#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    int n;
    cin >> n;
    vector<int> v(n);
    stack<int> s;

    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
        s.push(v[i]);
    }

    for (int i = 0; i < n; i++)
    {
        ;
    }
    return 0;
}