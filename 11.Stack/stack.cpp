//#10828
#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main(void)
{
    vector<int> stack(5);
    int top = -1;
    int num = 0;

    cin >> num;

    vector<string> command(14);

    for (int i = 0; i < num; i++)
    {
    }

    return 0;
}
